/** Shared pieces of the pill button used across the workspace. Padding is passed
 *  per use site because the prototype sizes them differently by context. */
export const pillBase =
  'inline-flex cursor-pointer items-center justify-center gap-[9px] rounded-full text-[13px] transition-colors'

export const pillPrimary = 'bg-chronos font-medium text-void hover:bg-chronos-hi'

export const pillGhost =
  'border border-line-strong text-sand hover:border-accent-line hover:text-paper'
